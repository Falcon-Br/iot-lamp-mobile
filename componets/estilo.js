import { StyleSheet } from "react-native";

export default StyleSheet.create({
    toggle:{
        width:200,
        resizeMode: 'contain'
    },

    container: {
        flex: 1,
        backgroundColor: '#000',
        alignItems: 'center',
        justifyContent: 'center',
    }
})